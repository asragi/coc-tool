import { Presenter } from '../../../utils/connect';
import { SkillContentViewProps } from './view';

interface Props {}

export const SkillContentPresenter: Presenter<
  Props,
  SkillContentViewProps
> = ({}: Props) => {
  return {};
};
