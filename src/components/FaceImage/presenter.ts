import { Presenter } from '../../utils/connect';
import { FaceImageViewProps } from './view';

interface Props {}

export const FaceImagePresenter: Presenter<
  Props,
  FaceImageViewProps
> = ({}: Props) => {
  return {};
};
